export const InfStasObj = ({stas}) => {
    return (
        <div>
            <div>{stas.id}</div>
            <div>{stas.name}</div>
            <div>{stas.age}</div>
            <div>{stas.profession}</div>
        </div>
    )
}
